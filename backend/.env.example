# PostgreSQL connection string.
# Get a free one from Render (New → PostgreSQL) or Neon (neon.tech), e.g.:
DATABASE_URL="postgresql://user:password@host:5432/taskly?sslmode=require"

PORT=4000
CORS_ORIGINS="http://localhost:3000"
